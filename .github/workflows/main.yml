name: Copy to EC2

on:
  push:
    branches:
      - main

jobs:
  copy_to_ec2:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'

    - name: Checkout code
      uses: ./.github/actions/checkout-custom  # Use the custom checkout action

    - name: Copy code to EC2
      run: |
        ssh -i ${{ secrets.SSH_PRIVATE_KEY }} ubuntu@${{ secrets.EC2_IP }} "mkdir -p /home/youruser/yourapp"
        scp -i ${{ secrets.SSH_PRIVATE_KEY }} -r . ubuntu@${{ secrets.EC2_IP }}:/home/youruser/yourapp
